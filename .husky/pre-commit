#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Monorepo pre-commit: lint + type-check the web app via turbo.
echo "🔍 Linting..."
pnpm turbo run lint --filter=web || exit 1

echo "📝 Type checking..."
pnpm turbo run type-check --filter=web || exit 1

echo "✅ Pre-commit checks passed!"
