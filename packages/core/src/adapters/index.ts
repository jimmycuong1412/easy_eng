/**
 * Adapter setters/getters only — safe to import from a Server Component (e.g.
 * the web app's core-bootstrap) without pulling client hooks into the RSC graph.
 */
export * from './supabase';
export * from './storage';
export * from './platform';
